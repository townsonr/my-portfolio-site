# Treat these as binary so Git never applies line-ending conversion (avoids
# corrupted PDFs/images/fonts on Windows when core.autocrlf is enabled).
*.pdf binary
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
